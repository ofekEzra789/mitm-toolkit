# VM4 - Ubuntu Desktop (Victim)
# Place this file at: /etc/dhcp/dhclient6.conf
#
# Intentionally empty - disables Rapid Commit (Option 14).
# Forces dhclient to use the full Solicit -> Advertise -> Request -> Reply
# handshake, which allows the attacker's Preference 255 to take effect.
